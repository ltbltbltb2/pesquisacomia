(()=>{const input=document.querySelector('#search');if(!input)return;document.querySelector('.search-controls').hidden=false;const normalize=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const cards=[...document.querySelectorAll('[data-search]')];const buttons=[...document.querySelectorAll('[data-filter]')];const status=document.querySelector('.results-count');status.hidden=false;let category='Todas';function filter(){const query=normalize(input.value.trim());let count=0;for(const card of cards){const match=(category==='Todas'||card.dataset.category===category)&&normalize(card.dataset.search).includes(query);card.hidden=!match;if(match)count++;}document.querySelector('.empty').hidden=count!==0;status.textContent=`${count} ${count===1?'pesquisa encontrada.':'pesquisas encontradas.'}`;}input.addEventListener('input',filter);for(const button of buttons)button.addEventListener('click',()=>{category=button.dataset.filter;for(const b of buttons)b.setAttribute('aria-pressed',String(b===button));filter();});})();
